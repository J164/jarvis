import { describe, it } from 'vitest';

describe('collection options', () => {
	it.todo('should disable promotion of values');
});

describe('collection creation options', () => {
	it.todo('should define a validator');
});
